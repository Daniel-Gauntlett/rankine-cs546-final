Welcome to the rankine room booker. To use this program: 
First, run the command npm i in this directory to install all necessary files.
Run npm seed to fill the databases up with the starting users, rooms, and events that are specified in seed.js. 
To start the website, run npm start. 